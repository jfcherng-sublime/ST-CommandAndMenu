[project]
name = "CommandAndMenu"
version = "0.0.0"
description = "Provide some commands and menus for my personal uses."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "more-itertools==11.*",
  "typing-extensions>=4.11",
]

[dependency-groups]
dev = [
  "mypy>=1.20",
  "pytest>=9.0.3",
  "ruff>=0.15",
]

[tool.uv]
default-groups = ["dev"]

[tool.mypy]
# ignore_missing_imports = False
check_untyped_defs = true
strict_optional = true
mypy_path = 'typings:stubs'
python_version = '3.13'

[[tool.mypy.overrides]]
module = ["plugin.libs.*"]
ignore_errors = true
ignore_missing_imports = true

[tool.pyright]
include = ['./']
exclude = [
  '**/__pycache__/',
  '**/node_modules/',
  # git-related
  '**/.git/',
  '**/br-*/',
  '**/branch-*/',
]
ignore = ['**/.venv', '**/libs']
stubPath = 'typings'
pythonVersion = '3.13'

[tool.ruff]
preview = true
line-length = 120
target-version = 'py313'
exclude = [
  "*/libs/*",
  ".git",
  ".mypy_cache",
  ".venv",
  ".venv-*",
  "branch-*",
  "stubs",
  "tests/files",
  "typings",
  "vendor",
  "venv",
  "venv-*",
]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "W",
  "I",
  "UP",
  "FURB",
  "SIM",
]
ignore = ["E203"]

[tool.ruff.lint.per-file-ignores]
"boot.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
